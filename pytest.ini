[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    -m "not integration"
markers =
    unit: Unit tests
    integration: Integration tests (require AWS credentials, skipped by default)

